# SPEC.md — SpraGo 仕様書

## 0. このドキュメントについて

これまで `docs/AppInfo.md`・`docs/MainQuiz.md`・`docs/MainQuizDesign.md`・`docs/Stage.md`・`docs/StageDesign.md`・`docs/MVPRequirements.md`・`docs/AppRoadmap.md` に企画・設計・仕様が分散し、どれが現時点で有効な決定かが分かりにくくなっていた。本ドキュメントは**現時点で有効な仕様・決定事項を一元化した「真実の源」**であり、上記ドキュメントと矛盾する場合は本ドキュメントを優先する。

上記の各ドキュメントは削除せず、詳細な設計の経緯・データモデル検討の参考資料として残す（特に `StageDesign.md` のテーブル設計の思考過程、`MainQuizDesign.md` の地域階層・国解放の設計案は、実装時に読む価値がある）。

実行計画・タスクは `TASKS.md`、日々の連携ルール・組織体制は `CLAUDE.md` / `docs/company/` を参照。本ドキュメントは2026-07-31時点のヒアリング（Owner⇄AI CEO Katsuhiro）に基づく。

## 1. サービスコンセプト

- サービス名: SpraGo（仮）
- コンセプト: 「興味が世界を広げ、世界が言葉を教えてくれる」— 世界を冒険しながら、自分だけの「世界図鑑」を完成させるRPG
- クイズ＝戦闘というゲーム構造。国旗・首都・地理・言語（あいさつ）のクイズに答えて経験値・コインを得て、レベルアップしながら世界中の国を巡る
- 対象ユーザー: 老若男女（詳細は[6-3. アクセシビリティ](#6-3-アクセシビリティ)参照）。家族単位のアカウント（1つのUserアカウント＝保護者、その配下に複数のUserProfile＝家族それぞれのプレイヤー、Netflix型）
- 詳細な世界観・原案: `docs/AppInfo.md`、`docs/MainQuiz.md`

## 2. 組織体制・意思決定ルール

体制の詳細は `docs/company/Organization.md` を参照。ここでは意思決定の運用ルールのみ記す。

### 2-1. デフォルト: 広範な委任

Ownerは日常的な実装判断・技術選定・タスクの進め方についてAI CEO（Katsuhiro＝Claude）に一任している。**質問せず進めて事後報告する**のが基本姿勢（2026-07-29 Owner確認）。

### 2-2. 例外: 必ず確認する領域

以下は「重要な意思決定」として、実行前にOwnerへ確認する（2026-07-31ヒアリングで確定）。

- 仕様の大枠変更、外部公開に関わる判断（本番リリース判断そのもの）
- コストを伴う施策（有料ツール導入、インフラのグレードアップ等）
- 決済・法務に関わる新しい仕組み（例: 新しい課金導線の追加）
- その他、後戻りが困難またはユーザー・第三者に影響する変更

判断に迷う場合は「これは重要な意思決定か」を自問し、Yesなら質問、Noなら進めて事後報告する。

### 2-3. Git運用

- コミットメッセージは `.gitmessage` フォーマット（`type(用途): summary`）、連番付き（例: `#00033: fix:...`）
- 機能ごとにブランチを切り、こまめにpush
- **マージはブランチ作成者（CEO）が直接 `main` へ行ってよい**（GitHub PRレビューは経由しない。2026-07-31 Owner確認、gh CLI未導入のため）。ただしテストが通った状態でのみマージする
- 詳細は `docs/company/DevelopmentProcess.md`

## 3. システム構成・技術スタック

| 領域 | 技術 | 備考 |
|---|---|---|
| Frontend | Next.js（App Router）/ React / TypeScript / Tailwind CSS | `frontend/` |
| Backend | Laravel（Laravel Sail）/ PHP | ルートは主に `routes/api.php` に直接クロージャで記述（Controller層は薄い） |
| DB | MySQL 8.4 | Laravel Sailコンテナ |
| 決済 | Stripe（Checkout、`stripe/stripe-php`） | 詳細は[4-6](#4-6-決済コイン購入) |
| コンテナ | Docker（Laravel Sail） | ローカル開発環境 |
| 本番環境 | AWS（想定、詳細未確定） | [6-5](#6-5-本番デプロイ)参照 |
| リポジトリ | GitHub（`katsuhiro1215/Spra-go`） | |

### 予算方針

インフラ費用・有料ツールは「月数千円程度なら必要に応じて使ってよい」（2026-07-31 Owner確認）。大きな費用が発生する選択（AWSの上位グレード、高額SaaS等）は事前確認する。

## 4. 機能仕様（現状ステータス）

凡例: ✅実装済み / ⚠️部分実装 / ❌未実装（計画あり） / 🚫スコープ外（意図的に対応しない）

### 4-1. 認証・アカウント

- ✅ Owner / Admin / User の独立ログイン（Laravel Breeze、guard分離）
- ✅ UserSchema（家族単位）→ UserProfile（プレイヤー、複数人）
- ✅ プロフィールの作成・選択・**編集・削除**（2026-07-29追加）
- ❌ 決済前の追加認証（パスワード再確認等）— [4-6](#4-6-決済コイン購入)参照

### 4-2. 冒険ループ（コア体験）

- ✅ 国選択（`/travel/[countryId]`）→ 難易度選択（初級/中級/上級）→ ステージ選択 → クイズ回答
- ✅ **ステージ選択の蛇行パス表示**（2026-07-31実装）: 単純なグリッドではなく、Owner提供の参考実装（`docs/content/test.tsx`、過去の別プロジェクトでDuolingoを参考に作成）を踏まえた蛇行パス＋アイコン（🔒ロック/✓クリア/👑ボス/⭐挑戦可）＋次に挑戦できるステージの「START」演出に刷新（`components/app/stage-path.tsx`、`play/[id]`・`travel/[countryId]`で共通利用）
- ✅ 難易度ロック: 前の難易度のボスステージクリアが次の難易度の解放条件
- ✅ ステージ内ロック: 前のステージクリアが次のステージの解放条件
- ✅ 正解/不正解演出（画面いっぱいのCorrect!!/Wrong演出）
- 🚫 国そのもののロック（航空券消費等）: 現行は「入口は自由」の設計を維持し、国は常に全解放。`docs/MainQuizDesign.md`が構想する国解放ゲートは採用しない方針で実装済み
- ✅ **ホーム画面の国一覧**（2026-07-31追加）: `GET /api/countries`はコンテンツが実際にある国のみを返す（Owner管理画面の全件一覧とは別）。Accept-Languageヘッダーから「たぶんこの国」を弱く推定し（IPジオロケーションは使わない、プライバシー・依存コストの理由）、`is_suggested`フラグを付けて先頭に表示。必ず手動で選び直せる
- ❌ **多言語対応・海外展開**: 「世界中の人が楽しめるように」という将来構想はあるが、UI翻訳・クイズ問題文の翻訳を伴う大きなスコープ拡大のため、ホーム画面改善とは切り離して別途スコープ決定する（未着手、`TASKS.md`参照）
- ✅ **全画面共通ヘッダー**（2026-07-31実装、Owner指摘により修正）: 以前はホーム画面にしかヘッダーが無く、クイズ等に遷移すると消えていた不具合を修正。`components/app/app-header.tsx`を新設し、ホーム/国詳細/国リビール/クイズ/ミニアプリ/ショップの全画面で共通表示（プロフィール切替・ログアウトのドロップダウンに旧「?」ヘルプボタンを統合。ヘルプ自体は未実装のため無効化表示）
- ✅ **ホーム画面のレイアウト整理**（2026-07-31実装）: 国選択（フラッグ円形表示がデフォルト、地図表示は切替式）を画面中央に集約し、ミニアプリ（ジャンル一覧）は右端タブから引き出すドロワーに変更。周辺要素を減らし主要な操作に集中できるようにした
- ✅ **効果音・BGM**（2026-07-31実装）: 正解/不正解/満点クリア時の効果音（`correct.mp3`/`incorrect.mp3`/`allCorrect.mp3`）と、クイズプレイ中のBGM（`bgm1.mp3`、ループ再生・音量35%）を`components/app/sound-provider.tsx`で管理。ブラウザの自動再生ポリシー（未クリック状態での音声再生禁止）に配慮し既定はOFF、右下のボタンでOn/Off切替・localStorageに保存（BGM/効果音は同一のOn/Offで連動、個別切替は現状無し）。音源ファイルは`public/sounds/`に配置済み（2026-07-31、Owner提供）

### 4-3. 経済ロジック（HP / XP / Coin / コンボ）

- ✅ HP: 初期20、正解-1・不正解-2、0未満にならない、レベルアップで全回復
- ✅ XP: 正解+10、`intdiv(xp,100)+1` でレベル算出
- ✅ Coin: 正解+5、ステージクリア+100
- ✅ コンボ: 連続正解数をカウント、5コンボごとに+20コインのボーナス。不正解でリセット（ベストコンボは保持）
- ✅ **ストリーク**（2026-07-31実装）: 毎日プレイ（問題に1問でも回答）で継続日数をカウント、7日ごとに+50コインのボーナス。日の境界はAsia/Tokyo固定（多言語対応でユーザーごとのタイムゾーンに分ける場合は要見直し）。1日でも空くと1にリセット（ベストストリークは保持）。同じ日に何問答えても加算は1日1回のみ

### 4-4. コンテンツ（クイズ）

- ✅ 4ヶ国（日本・アメリカ・イギリス・フランス）× 3難易度（初級・中級・上級）、計728問
- ✅ 出題テーマ: 国旗→国名、国名→国旗、地理、首都、言語（あいさつ）の5種
- ✅ `content:import` コマンドによる原稿取り込み（コンテンツ制作担当が作成したJSON原稿を検証の上、DBへ反映）
- ⚠️ **未レビュー**: 728問は生成AIによるリサーチ＋CEO(Claude)のスポットチェックのみで、人間（Owner）による本格的なファクトチェックを経ていない。**本格公開前にOwnerが全問レビューする**（2026-07-31確認、[6-2](#6-2-コンテンツ品質保証)参照）
- ❌ 対象国の拡大: 本格公開前にあと2〜3ヶ国追加し、計7〜8ヶ国規模にする（2026-07-31確認、国は未選定）
- ❌ 国旗以外の入口（動物・食べ物・スポーツ・歴史・世界遺産・音楽等）: `Category`のルートカテゴリーはあるが中身のStageが無い
- ❌ 地域階層（地方→都道府県→都市）: `Region`モデル・API・画面はあるが、実コンテンツはほぼ国レベルのみ（`region_id`を持つStageは1件のみ）

### 4-4a. 言語学習モード（2026-07-31追加、公開前必須項目より優先）

Owner確認: 「国について学ぶ（トリビア）」と「その国の言語を学ぶ（言語学習）」の2モード制を導入し、`SPEC.md` 6-4/6-2の法務文書整備・728問レビューより**優先して**着手する。

- 構造: 国ごとに「〇〇について学ぶ」（既存のトリビア、4-4）と「〇〇語を学ぶ」（新規、言語学習）の2択。言語学習モードは対象言語のコンテンツが存在する国のみ表示する（例: 日本語話者が主対象の現状では、日本を選んだ際の「日本語を学ぶ」は非表示。将来の海外展開（7章）が具体化した際に有効化を検討）
- 技術方針: 新しいエンジンは作らず、既存の`Category`（トピック軸）× `Stage`（難易度・ステージ）の仕組みをそのまま流用。`categories.is_language_mode`フラグで言語学習系カテゴリーを区別し、`/api/countries/{country}`が返す`groups`をフロント側でトリビア/言語学習に振り分けて表示する
- 出題形式: 既存の4択クイズ形式（`Question`/`Choice`）をそのまま使用。出題テーマを追加（単語 `vocabulary`・フレーズ `phrase`・文法 `grammar`）
- パイロット範囲（2026-07-31 Owner確認）: 英語（アメリカ・イギリス）のみ、まず作り込む。他言語への展開は本パイロットの結果を見て判断
- ✅（2026-07-31実装）カテゴリー・出題テーマのDB追加（`categories.is_language_mode`、ルートカテゴリー「英語を学ぶ」、出題テーマ`vocabulary`/`phrase`/`grammar`）、国選択→演出→リビール→モード選択のフロントUI（`/travel/[countryId]/start`）、`travel/[countryId]`のトリビア/言語学習タブ振り分け
- ✅（2026-07-31実装）ホーム画面の国選択をデスクトップ幅では世界地図（`WorldMap`コンポーネント、`public/map/world.svg`）に変更。コンテンツがある国だけ色付けしてクリック可能にする。出典はWikimedia Commonsのパブリックドメイン画像（`public/map/README.md`参照）。モバイル幅は従来のグリッド表示を維持
- ✅（2026-07-31実装）アメリカ向け英語学習コンテンツ（初級、9ステージ・50問、単語・フレーズ中心、ボスステージで称号「英語はじめの一歩」）
- ✅（2026-07-31実装）イギリス向け英語学習コンテンツ（初級、9ステージ・54問。アメリカ英語との綴り・語彙差（colour/color、biscuit/cookie、lorry/truck等）を意識した差別化コンテンツ、ボスステージで称号「イギリス英語マスター見習い」）
- ✅（2026-07-31実装）中級・上級への拡張: アメリカ・イギリスそれぞれ中級9ステージ・54問、上級9ステージ・54問。出題テーマに`grammar`（文法）を追加し、be動詞〜仮定法まで難易度に応じた文法を出題。ボス称号はアメリカ「英語中級への扉」「英語マスター」、イギリス「イギリス英語中級への扉」「イギリス英語マスター」
- 🐛（2026-07-31修正）イギリスの国コードがDB上`GB`（大文字）で、`/flag/{code}.svg`が小文字ファイル名前提だったため国旗が表示されない不具合があった。`public/flag/GB.svg`を追加して解消（国コードの大文字/小文字が国によって不揃いな点は未整理のまま残っている）
- 🐛（2026-07-31修正）**重要**: `stages`テーブルの一意制約が`category_id`+`difficulty`+`stage_number`のみで`country_id`を含んでいなかったため、言語学習モードのように複数国（米国・英国）が同じカテゴリーを共有すると、`content:import`の`updateOrCreate`が他国のステージを上書きしてしまう不具合があった。一意制約に`country_id`を追加するマイグレーションで修正し、`ImportContentCommand`のステージ検索条件にも`country_id`を追加した。中級・上級コンテンツ投入時に発覚し、データを一度削除して全ファイル再取り込みで復旧済み

### 4-4b. マイパスポート（2026-07-31追加、UIモックアップ段階）

Owner構想:「旅の成果がすべて残る場所」。世界地図が埋まる・スタンプが増える・鍵が開く・航空券が集まる・図鑑が完成する・称号が増える・旅の思い出が残る、の7要素を1画面に集約する。

- 各要素は新エンジンを作らず既存データにマッピングする方針: 世界地図→`WorldMap`再利用、スタンプ→難易度クリア状況（🥉🥈🥇）、鍵→既存の難易度ロック機構の可視化、航空券→訪問国数の実績表示、図鑑→国別カード（`mood_emoji`等）、称号→既存`profile_titles`の一覧化（現状一覧UIが無いため新規）、思い出→`ProfileStageProgress.cleared_at`の記録
- ✅（2026-07-31実装）`/passport`にUIモックアップを実装（`AppHeader`に📔リンク追加）。**現時点ではモックデータ表示のみで、実データ配線は未着手**（デザインの方向性が固まってから`/api/passport`等を新設する）
- ❌ 実データ配線、称号一覧の集計ロジック、航空券・鍵のプレイヤーごとの実績計算

### 4-5. ショップ

- ✅ `potion`（回復薬、HP回復）: 実装済み
- ✅ `title`（称号）: 購入すると`profile_titles`に付与（ボス称号と同じ仕組みを再利用）
- 🚫 `plane` / `background` / `character`: プレイヤー向け一覧・購入APIから除外済み（`config/shop.php`の`enabled_types`）。地域ロックの再設計やキャラクター/背景アセットの整備が前提となるため、現時点では実装しない

### 4-6. 決済（コイン購入）

- ✅ Stripe Checkoutによるコイン購入（`config/coin_packages.php`で価格をサーバー側管理、フロントの金額は信用しない設計）
- ✅ Webhook（署名検証必須、`stripe_checkout_session_id`で二重付与防止）
- ⚠️ 現在は開発用Stripeテストキー（`sk_test_`）のみ。本番キーへの切替時期は未定（[6-5](#6-5-本番デプロイ)参照）
- ⚠️ `STRIPE_WEBHOOK_SECRET`が`.env`未設定のため、決済完了→コイン付与の一気通貫の動作確認は未実施
- ❌ **未成年の課金安全策**: UserProfile（プレイヤー、子どもを含む）とUser（決済を行う保護者アカウント）は分離されているが、購入前の追加確認は未実装。**購入前にパスワード再確認等の追加ステップを入れる方針**（2026-07-29確認、具体的なタイミング・UXは実装時に設計）
- ❌ 返金ポリシー: 未策定（[6-4](#6-4-法務ドキュメント)参照）
- 価格（仮）: 100枚/¥120、550枚/¥500（+10%）、1200枚/¥1000（+20%）。正式な価格戦略は未確定

### 4-7. マーケティングサイト（アプリ本体とは別領域）

Webサイト関連の機能は「随時進めてよい」（2026-07-31確認）領域。詳細方針は `docs/company/MarketingPlan.md`。

- ✅ ゲスト向けLP（トップページ）
- ✅ 国別お試しクイズページ（`/world/[code]`、認証不要、SEO向け、3問体験版）
- ✅ ブログ基盤（`/blog`、型付き配列で記事管理、記事1本公開済み）
- ✅ 会社情報ページ（`/about`）

### 4-8. Owner管理画面

- ✅ カテゴリー/国/言語/地域/クイズ/ステージ/出題テーマ/ショップ/イベント/コンテンツ/Admin/Userの管理CRUD
- ✅ サイドナビのグループ化（冒険データ/クイズ運用/収益・運営/システム）
- ✅ ダッシュボードKPIカード（2026-07-31実装）: 登録ユーザー数・プレイヤー数・直近7日の新規登録/ステージクリア数・コンテンツがある国の数・コイン購入件数と当月売上を表示（`GET /api/owner/dashboard/summary`）
- ❌ AI生成機能: ナビ項目のみ存在、中身はスタブ
- ⚠️ イベント（2026-07-31方針決定）: `Event`モデルは`title`/`starts_at`/`ends_at`のみで、ボーナス倍率や特別コンテンツとの紐付けが一切無い（期間ラベルだけの器）。プレイヤー向け表示は**当面作らない**（実装するには「イベントが何をするか」という企画がまず必要なため）。Owner画面には「まだプレイヤーには反映されない」旨の注記を追加し、誤解を防止した。プレイヤー向け実装は企画が固まってから再検討

## 5. データモデル概要

主要テーブルと役割（詳細は各マイグレーション・モデルを参照）。

- `users` / `admins` / `owners`: 認証（guard分離）
- `user_schemas`（家族）→ `user_profiles`（プレイヤー、hp/xp/coins/level/combo/best_combo）
- `categories`（トピック軸、階層あり）/ `countries` / `regions`（地理軸、現状ほぼ未使用）/ `languages`
- `quizzes` → `questions` → `question_choices`、`question_themes`（出題の角度）
- `stages`（category_id・country_id・region_id・difficulty・stage_number・is_boss・title_reward）、`stage_questions`（中間テーブル、出題プールを事前確定）
- `profile_stage_progress`（進捗の唯一の情報源、挑戦時にのみ行を作るスパース設計）
- `profile_titles`（称号の付与記録）、`profile_currency_ledger`（HP/XP/Coin増減の台帳）
- `shop_items` / `user_profile_items`（購入記録）
- `coin_purchases`（Stripe決済の記録、`stripe_checkout_session_id`で冪等性を担保）
- `events` / `content_items`: Owner側で管理可能だが、プレイヤー向け導線は未実装

設計思想（`docs/StageDesign.md`より継承）:

- 出題プールの確定はOwner操作時に1回（`stage_questions`）。プレイ時は`ORDER BY RAND()`を使わずインデックス検索のみ、シャッフルはPHP側
- 進捗・実績はスパース設計（挑戦した分だけ記録）で書き込み・容量を抑える

## 6. 非機能要件

### 6-1. テスト方針

- **バックエンド**: Pestによる機能テストを標準とする。経済ロジック・決済・コンテンツ取り込み・難易度ロック等、お金とゲームバランスに関わるロジックは実装と同時にテストを書く（2026-07-31時点で52件）
- **フロントエンド**: 現状 `npm run lint` / `npm run typecheck` のみで自動テストは0件。**本格公開前に、決済フロー等の重要フローに限定した最低限のE2Eテスト（Playwright等）を整備する**（2026-07-31確認）。全画面を網羅する自動テストは現時点では優先しない
- コミット前チェックの詳細は `docs/company/DevelopmentProcess.md`

### 6-2. コンテンツ品質保証

- クイズ原稿はコンテンツ制作担当（AIサブエージェントによるWeb検索リサーチ）が作成し、CEO（Claude）がスポットチェックした上でDBへ取り込む運用としてきた
- **本格公開（不特定多数の一般ユーザー向け）前には、Ownerによる全問レビューを必須とする**（2026-07-31確認）。現在DBにある728問（4ヶ国×3難易度）はこの人間チェックをまだ経ていない
- 今後追加するコンテンツ（国の拡大分含む）も同様に、公開前のOwnerレビューを経る

### 6-3. アクセシビリティ

「老若男女」を対象とする前提に対し、現状の実装は配慮が一切ない状態だった。**本格対応が必要**（2026-07-31確認）。必須項目:

- 漢字のふりがな（ルビ）表示・切替機能
- 文字サイズ切替機能
- 色弱・色の見え方の多様性に配慮した配色・コントラスト（正解/不正解等を色だけに依存させない、形・アイコンも併用）
- 操作性: キーボード操作対応、スクリーンリーダー対応（ARIA属性等）

適用範囲: **新規実装から適用し、既存画面（ログイン・クイズ画面等）は後で段階的に直す**（ローンチブロッカーにはしない）。具体的な準拠水準（WCAG 2.1 AA等）は未確定、実装を進めながら次ラウンドで詰める。

**2026-07-31 実装状況（基盤）**:

- ✅ 文字サイズ切替（標準/大/特大、`AccessibilityProvider`が`<html data-font-scale>`を制御、localStorageに保存）
- ✅ ふりがな表示切替（`Furigana`コンポーネント、ネイティブ`<ruby>/<rt>`使用、追加依存なし）。静的なUI文言（ホーム/プロフィール選択の見出し、難易度ラベル等）は手動で適用
- ✅ **クイズ本文への自動ふりがな**（2026-07-31実装）: 728問のprompt/choiceテキストは書き換えず、辞書ベースの自動変換方式を採用。実コンテンツから抽出した漢字語彙600語をコンテンツ制作担当が読み調査し`frontend/src/lib/furigana-dictionary.json`として整備、表示側（`components/app/auto-furigana.tsx`）で最長一致により`<ruby>`へ自動変換。クイズ画面・公開プレビュー（`/world/[code]`）に適用済み。将来コンテンツが増えた際も辞書を拡張すれば追従できる設計
- ✅ 色弱配慮の第一弾: クイズ回答後の選択肢に✓/✕アイコンを追加し、正解/不正解の判別を色だけに依存させないようにした
- 画面右下に共通コントロール（`AccessibilityControls`）を全ページ共通で設置
- ❌ 未着手: 既存画面全体のARIA属性監査、スクリーンリーダーでの通し確認、WCAG準拠水準の正式決定

### 6-4. 法務ドキュメント

利用規約・プライバシーポリシー・返金ポリシーは現状すべて未整備。子どものデータを扱い、実際の決済も発生するサービスであるため、**本格公開（不特定多数の一般ユーザー向け）前の必須タスク**とする（2026-07-31確認、`TASKS.md`参照）。ドラフトはCEOが作成できるが、法的な内容のため人間（Owner、必要に応じて専門家）の確認を要する。

### 6-5. 本番デプロイ

AWS想定だが、具体的な構成（サービスグレード、ドメイン、HTTPS、Stripe本番キーへの切替時期等）はまだ仮の想定でよく、**詳細設計はローンチ判断が近づいてから**行う（2026-07-31確認）。`TASKS.md`では「本番デプロイ」を大枠のタスクとして置き、詳細はブレイクダウンしない。

## 7. スコープ外・将来検討（現時点で対応しない）

- AI問題生成・AI先生・AI会話、音声認識
- 有料メンバーシップ（プラン制、コイン購入とは別軸）
- パスポート専用画面（世界図鑑の集約ビュー）、旅演出（国到着シーン）
- フレンド機能、家族ランキング
- 学校向け・企業向けプラン
- スマホアプリ化
- 本格的なビジュアルデザイン（イラストレーター/デザイナーによる作り込み）: 現行のSceneBackground・絵文字風キャラクター・AppButtonの3D押下ボタンは**暫定の見た目**であり、将来本格的なデザイナーを入れる前提（2026-07-31確認）

## 8. 参照ドキュメント一覧

| ドキュメント | 位置づけ |
|---|---|
| `CLAUDE.md` | 体制・連携の入り口 |
| `SPEC.md`（本書） | 仕様の真実の源 |
| `TASKS.md` | 実行管理（部門別タスク分解） |
| `docs/company/Organization.md` | 組織図・役割・意思決定フロー |
| `docs/company/BusinessPlan.md` | 事業計画・目標 |
| `docs/company/DevelopmentProcess.md` | 開発フロー・Git運用 |
| `docs/company/MarketingPlan.md` | マーケティング方針 |
| `docs/content/README.md` | コンテンツ制作パイプライン |
| `docs/AppInfo.md` / `docs/MainQuiz.md` / `docs/MainQuizDesign.md` | サービス原案・「メインクエスト」構想（一部未採用・将来検討） |
| `docs/Stage.md` / `docs/StageDesign.md` | ステージ機能の原案・詳細設計（実装済み部分の経緯） |
| `docs/MVPRequirements.md` | 初期MVP要件定義（歴史的経緯、一部は本書で更新） |
| `docs/AppRoadmap.md` | 実装ギャップの棚卸し記録（本書と合わせて参照） |
| `docs/design/ui-ux-proposal.md` | UI/UX改善提案 |
